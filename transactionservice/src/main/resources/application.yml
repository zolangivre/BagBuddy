spring:
  application:
    name: transaction-service

  datasource:
    url: jdbc:postgresql://${TRANSACTION_SERVICE_DB_HOST:bagbuddy-transaction-service-postgres}:5432/${TRANSACTION_SERVICE_DB:transactionservice}
    username: ${TRANSACTION_SERVICE_USER:transactionuser}
    password: ${TRANSACTION_SERVICE_PASSWORD:transactionpass}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082

eureka:
  instance:
    hostname: transaction-service
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

logging:
  level:
    org.hibernate.SQL: debug
    org.springframework: info
